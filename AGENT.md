# AGENT.md

> Alias for `CLAUDE.md` — the canonical doc for the Voice Voyage Services monorepo.
> `AGENT.md` exists because some harnesses look for `AGENT.md` by name; the single source of truth remains `CLAUDE.md` in this folder. Edits should land in `CLAUDE.md` and be copied here.

— See `CLAUDE.md` for full guidance (two services, cross-service contract, commands, architecture, deployment & hardware specs, HF free-tier notes, hot-reload workflow, tests).
